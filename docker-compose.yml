services:
  price-tracker-bot:
    build: .
    container_name: price-tracker-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persiste la base de datos con tu histórico de precios.
      - ./data:/app/data
